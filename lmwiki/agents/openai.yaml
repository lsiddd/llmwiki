interface:
  display_name: "LMWiki"
  short_description: "Build and maintain markdown knowledge wikis"
  default_prompt: "Use $lmwiki to create, ingest, query, research, lint, or export my markdown knowledge wiki."

policy:
  allow_implicit_invocation: true
